{
  "name": "firefox-lead-extractor",
  "version": "1.0.0",
  "private": true,
  "description": "A local-first Firefox lead extractor extension",
  "scripts": {
    "test": "node --test tests/*.test.js"
  }
}
