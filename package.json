{
  "name": "leadfox",
  "version": "2.0.0",
  "license": "MIT",
  "private": true,
  "description": "A local-first Firefox lead extractor extension",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "lint": "web-ext lint --source-dir . --warnings-as-errors",
    "build": "web-ext build --source-dir . --artifacts-dir dist --overwrite-dest"
  },
  "devDependencies": {
    "web-ext": "10.6.0"
  }
}
